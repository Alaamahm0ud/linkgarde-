app:
  name: "LINKGARDE"
  version: "1.0.0"
  api_host: "0.0.0.0"
  api_port: 8080

redis:
  host: "localhost"
  port: 6379
  db: 0

models:
  nlp_model: "distilbert-base-uncased"
  vision_model: "simulated-vision-detector"

security:
  allowed_hosts:
    - "localhost"
    - "127.0.0.1"
  cache_ttl: 3600
